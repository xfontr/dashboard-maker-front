const ENDPOINTS = {
  tokens: {
    verify: "tokens/verify",
  },
  users: {
    getAll: "users/",
    signUp: "users/",
    logIn: "users/log-in",
    refreshToken: "users/refresh",
  },
};

export default ENDPOINTS;
